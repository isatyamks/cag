version: '3.8'

services:
  app:
    build: .
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    volumes:
      - ./cag_cache:/app/cag_cache
      - ~/.cache/huggingface:/root/.cache/huggingface
    stdin_open: true
    tty: true
